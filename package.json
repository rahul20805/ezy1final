{
  "name": "@caffeine/template-app",
  "type": "module",
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=7.0.0",
    "npm": "please use pnpm"
  },
  "scripts": {
    "dev": "npm --prefix src/frontend run dev",
    "build": "npm --prefix src/frontend run build",
    "typecheck": "npm --prefix src/frontend run typecheck",
    "check": "npm --prefix src/frontend run check",
    "fix": "npm --prefix src/frontend run fix"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
  },
  "dependencies": {}
}
